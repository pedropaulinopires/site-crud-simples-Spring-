server:
  error:
    include-stacktrace: never
spring:
  datasource:
    url: jdbc:mysql://localhost:3303/spring
    username: root
    password: 92470
    name: mysql
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true